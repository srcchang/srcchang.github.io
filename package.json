{
  "name": "srcchang-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "node scripts/build-all.mjs",
    "dev:hub": "npm run dev -w apps/hub",
    "dev:salary": "npm run dev -w apps/salary_thief",
    "dev:voiback": "npm run dev -w apps/voiback",
    "preview": "npx -y serve dist"
  },
  "dependencies": {
    "@iconify-json/tabler": "^1.2.35",
    "astro-seo": "^1.1.0"
  }
}
